server:
  port: 8080

spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/DarthDS4711/MicroServicios
          search-paths: config-data
  application:
    name: ConfigServer